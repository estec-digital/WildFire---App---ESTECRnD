Instruction to run code:

============================================================
Option 1: Run in Terminal with administrator privilage:

$ python main.py config=< Absolute path to input.json file>

Ex: $ python main.py config=D:\ChayRungProject\FireDetector\input.json

if not, you can run as default: $python main.py
Adjust variables inside: input.json file as you want.

======================================================
Option 2: under development in Window OS
Only for DEBUG mode service can run
You can run by using service:
1. Initialize service 
	$ python AI_engine_service.py install
2. Starting service
	$ python AI_engine_service.py start 
3. Stop service
	$ python AI_engine_service.py stop
4. Debug service 
	$ python AI_engine_service.py debug
5. Remove service
    $ python AI_engine_service.py remove

5. Input variable or argument in file: "input.json" 

=======================================================
Kill all processes and background
$ taskkill /f /im python.exe /t
